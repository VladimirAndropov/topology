

# 📘 СЕМИНАР ПО PYTHON (Темы 1.1–2.4)

---

# 1️⃣Введение

* Python создан Гвидо ван Россум.
* Название связано с шоу Monty Python’s Flying Circus.
* Python:

  * интерпретируемый
  * динамически типизированный
  * объектно-ориентированный
* Всё в Python — **объекты**.
* Переменные — это **ссылки на объекты**, а не контейнеры значений.

---


## ⚙ Ассемблер

```
mov eax, [x]  
mov [y], eax
```
Здесь:

1.  Значение из памяти по адресу `x` копируется в регистр
2.  Потом копируется в память по адресу `y`

Получается **две независимые области памяти**.


## 🐍 Python
```
a = 10  
b = a
```
Это:
```
a ─┐  
   ├──► [10]  
b ─┘
```

Нет копирования числа (для неизменяемых объектов это логическая модель ссылок).

* * *

# 2️⃣ Числовые типы
# Присваивание
## 🔹 int (целые числа)

```python
x = 10
y = -3
z = 10_000_000
print(type(x))
print(id(x))
```


### Системы счисления

```python
a = 0b1010   # двоичная → 10
b = 0o12     # восьмеричная → 10
c = 0xA      # шестнадцатеричная → 10

print(a, b, c)
```

---

## 🔹 float (вещественные)

```python
x = 3.14
y = 1e-3
print(type(x))
```

---

## 🔹 complex (комплексные)

```python
z = 2 + 3j
print(type(z))
```

---

# 3️⃣ Арифметические операции

```python
print(2 + 3)
print(5 - 2)
print(4 * 3)
print(10 / 3)   # всегда float
print(10 // 3)  # целая часть
print(10 % 3)   # остаток
print(2 ** 3)
```

📌 Важно объяснить:

* `/` → всегда float
* `//` → целочисленное деление
* `%` → остаток

---

# 🎯 Практика (с решениями)

### Задание 1

Найти остаток от деления 123 на 10.

```python
result = 123 % 10
print(result)
```

---

### Задание 2

Возвести 7 в степень 3.

```python
print(7 ** 3)
```

---

### Задание 3

Проверить тип результата 10 / 2.

```python
value = 10 / 2
print(value)
print(type(value))
```

---

# 4️⃣ Преобразование типов

```python
print(int("42"))
print(float("3.14"))
print(str(255))
```

### Ошибка:

```python
# int("10.5")  # ValueError
```

Правильный способ:

```python
value = int(float("10.5"))
print(value)
```

---

# 🎯 Практика

### Преобразовать строку "3.14" в float

```python
s = "3.14"
print(float(s))
```

---

### Преобразовать число 255 в строку

```python
num = 255
text = str(num)
print(text)
```

---

# 5️⃣ Динамическая типизация

```python
x = 10
print(type(x))

x = "hello"
print(type(x))
```

📌 Тип определяется во время выполнения.

---

# 6️⃣ Переменные и ссылки

## 🔹 Всё — объекты

```python
a = 5
b = a

print(a == b)
print(a is b)
```
### Схема

```text
┌─────┐
│  a  │
└──┬──┘
   │
   │
┌──▼──┐
│     │
│  10 │   (один объект)
│     │
└──▲──┘
   │
┌──┴──┐
│  b  │
└─────┘
```

👉 Два имени → один объект
---

## 🔹 Изменяемые vs неизменяемые

### Неизменяемые:

* int
* float
* str
* tuple

```text
        _________
     .-"         "-.
    /               \
   /                 \
  |      __________    |
  |     /          \   |
  |    |     10     |  |
  |     \__________/   |
   \                   /
    \                 /
     "-.___________.-"
```

### Изменяемые:

* list
* dict
* set

---

## 🔹 Ключевой пример (обязательно разобрать)

```python
list1 = [1, 2]
list2 = list1

list1.append(3)

print(list1)
print(list2)
print(list1 is list2)
```

Объяснить:
Обе переменные указывают на один объект.

### Схема

```text
list1 ─┐
       │
       ▼
      ┌─────────────────┐
      │ type: list      │
      │ value: [1,2,3]  │
      └─────────────────┘
       ▲
       │
list2 ─┘
```

👉 Один объект

---

## 🔹 Как создать копию

```python
list1 = [1, 2]
list2 = list1.copy()

list1.append(3)

print(list1)
print(list2)
```

---

# 🎯 Практика

1. Создать список.
2. Присвоить второй переменной.
3. Изменить через первую.
4. Проверить `is`.

---

# 7️⃣ Строки

## 🔹 Создание

```python
s1 = "Hello"
s2 = 'World'
```

---

## 🔹 Строки неизменяемы

```python
s = "Hello"
# s[0] = "h"  # ошибка
```

---

## 🔹 Индексация

```python
s = "Python"

print(s[0])
print(s[-1])
```

---

## 🔹 Срезы

```python
s = "Programming"

print(s[:4])
print(s[3:])
print(s[::-1])
```

---

# 🎯 Практика

### Вывести последний символ

```python
s = "Algorithm"
print(s[-1])
```

---

### Развернуть строку

```python
s = "Python"
print(s[::-1])
```

---

### Получить первые 4 символа

```python
s = "Algorithm"
print(s[:4])
```

---

# 8️⃣ Форматирование строк

## 🔹 f-строки (рекомендуемый способ)

```python
name = "Иван"
age = 20

print(f"Меня зовут {name}, мне {age} лет")
print("Меня зовут {}, мне {} лет".format(name, age))
```

---

## 🔹 Форматирование чисел

```python
pi = 3.1415926
print(f"{pi:.2f}")
```

---

# 🎯 Практика

### Вывести число 3.14159 с точностью до 3 знаков

```python
pi = 3.14159
print(f"{pi:.3f}")
```

---

### Сформировать строку

```python
name = "Иван"
score = 95

print(f"Студент {name} получил {score} баллов")
```

---

# 9️⃣ Работа с input()

```python
name = input("Введите имя: ")
age = int(input("Введите возраст: "))

print(f"Привет, {name}! Через год тебе будет {age + 1}.")
```

📌 Объяснить:

* input() всегда возвращает строку
* нужно использовать int()

---

# 🔟 Мини-проект с ошибкой для семинара

```python
name = input("Введите имя: ")
birth_year = input("Введите год рождения: ")

current_year = 2024
age = current_year - birth_year

print("{}, вам примерно {} лет.")
```

Разобрать:

* преобразование типов
* арифметику
* форматирование

---

# 1️⃣1️⃣ Быстрые контрольные вопросы

1. Чем отличается `is` от `==`?
2. Почему 10 / 5 → 2.0?
3. Какие типы неизменяемые?
4. Что хранится в переменной?
5. Почему изменение списка видно через вторую переменную?



